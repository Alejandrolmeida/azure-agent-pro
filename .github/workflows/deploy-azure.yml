name: Deploy to Azure

# DISABLED: This workflow is for educational purposes only
# Requires Azure credentials and resources to be configured
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - test
          - stage
          - prod
      confirm_production:
        description: 'Confirm production deployment (type "CONFIRM" for prod)'
        required: false
        default: ""

env:
  # Configure these values for your environment
  PROJECT_NAME: azure-agent-pro # Azure Agent Pro project name
  # Note: These would be configured in a real Azure environment
  # AZURE_LOCATION: ${{ vars.DEFAULT_LOCATION || 'eastus' }}
  AZURE_LOCATION: "eastus"

jobs:
  deploy-dev:
    name: Deploy to Development
    runs-on: ubuntu-latest
    # DISABLED for educational project - requires real Azure credentials and resources
    if: false # Change to 'true' and configure Azure secrets to enable
    # environment: development  # Commented out to hide deployments section

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Azure CLI
        run: |
          curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
          az version

      - name: Azure Login using OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Create Resource Group
        run: |
          RG_NAME="${{ env.PROJECT_NAME }}-dev-rg"
          echo "RESOURCE_GROUP=$RG_NAME" >> $GITHUB_ENV
          az group create --name "$RG_NAME" --location ${{ env.AZURE_LOCATION }}

      - name: Deploy Infrastructure
        run: |
          echo "🚀 Deploying to Development environment..."
          RG_NAME="${{ env.PROJECT_NAME }}-dev-rg"
          az deployment group create \
            --resource-group "$RG_NAME" \
            --template-file bicep/main.bicep \
            --parameters @bicep/parameters/dev.parameters.json \
            --parameters deploymentTimestamp="$(date -u +%Y%m%d-%H%M%S)" \
            --name "dev-deployment-${{ github.run_number }}"

      - name: Get Deployment Outputs
        run: |
          echo "📋 Getting deployment outputs..."
          RG_NAME="${{ env.PROJECT_NAME }}-dev-rg"
          az deployment group show \
            --resource-group "$RG_NAME" \
            --name "dev-deployment-${{ github.run_number }}" \
            --query properties.outputs

  deploy-environment:
    name: Deploy to Selected Environment
    runs-on: ubuntu-latest
    # DISABLED for educational project - requires real Azure credentials and resources
    if: false # Change to 'true' and configure Azure secrets to enable
    # environment: ${{ inputs.environment }}  # Commented out to hide deployments section

    steps:
      - name: Validate Production Deployment
        if: inputs.environment == 'prod'
        run: |
          if [ "${{ inputs.confirm_production }}" != "CONFIRM" ]; then
            echo "❌ Production deployment requires confirmation. Please type 'CONFIRM' in the confirm_production input."
            exit 1
          fi
          echo "✅ Production deployment confirmed"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Azure CLI
        run: |
          curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
          az version

      - name: Azure Login using OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Set Environment Variables
        run: |
          ENV="${{ inputs.environment }}"
          RG_NAME="${{ env.PROJECT_NAME }}-${ENV}-rg"
          echo "RESOURCE_GROUP=$RG_NAME" >> $GITHUB_ENV
          echo "ENVIRONMENT=$ENV" >> $GITHUB_ENV

          # Set location based on environment - customize as needed
          case $ENV in
            prod|stage)
              echo "AZURE_LOCATION=eastus" >> $GITHUB_ENV
              ;;
            test)
              echo "AZURE_LOCATION=westus2" >> $GITHUB_ENV
              ;;
            *)
              echo "AZURE_LOCATION=${{ env.AZURE_LOCATION }}" >> $GITHUB_ENV
              ;;
          esac

      - name: Create Resource Group
        run: |
          az group create --name "$RESOURCE_GROUP" --location "$AZURE_LOCATION"

      - name: Pre-deployment Validation
        run: |
          echo "🔍 Running pre-deployment validation..."
          az deployment group validate \
            --resource-group "$RESOURCE_GROUP" \
            --template-file bicep/main.bicep \
            --parameters @"bicep/parameters/${ENVIRONMENT}.parameters.json" \
            --parameters deploymentTimestamp="$(date -u +%Y%m%d-%H%M%S)"

      - name: Deploy Infrastructure
        run: |
          echo "🚀 Deploying to $ENVIRONMENT environment..."
          az deployment group create \
            --resource-group "$RESOURCE_GROUP" \
            --template-file bicep/main.bicep \
            --parameters @"bicep/parameters/${ENVIRONMENT}.parameters.json" \
            --parameters deploymentTimestamp="$(date -u +%Y%m%d-%H%M%S)" \
            --name "${ENVIRONMENT}-deployment-${{ github.run_number }}" \
            --verbose

      - name: Post-deployment Verification
        run: |
          echo "✅ Verifying deployment..."

          # Check if resources were created successfully
          DEPLOYMENT_STATUS=$(az deployment group show \
            --resource-group "$RESOURCE_GROUP" \
            --name "${ENVIRONMENT}-deployment-${{ github.run_number }}" \
            --query properties.provisioningState -o tsv)
            
          if [ "$DEPLOYMENT_STATUS" != "Succeeded" ]; then
            echo "❌ Deployment failed with status: $DEPLOYMENT_STATUS"
            exit 1
          fi

          echo "✅ Deployment completed successfully!"

      - name: Get Deployment Outputs
        run: |
          echo "📋 Deployment outputs:"
          az deployment group show \
            --resource-group "$RESOURCE_GROUP" \
            --name "${ENVIRONMENT}-deployment-${{ github.run_number }}" \
            --query properties.outputs --output table

      - name: Tag Resources
        run: |
          echo "🏷️ Tagging resources..."
          az group update \
            --name "$RESOURCE_GROUP" \
            --set tags.Environment="$ENVIRONMENT" \
            tags.DeployedBy="GitHub-Actions" \
            tags.Repository="${{ github.repository }}" \
            tags.CommitSha="${{ github.sha }}" \
            tags.DeploymentDate="$(date -u +%Y-%m-%d)" \
            tags.RunNumber="${{ github.run_number }}"

  notify-deployment:
    name: Notify Deployment Status
    runs-on: ubuntu-latest
    needs: [deploy-environment]
    if: always() && github.event_name == 'workflow_dispatch'

    steps:
      - name: Deployment Success
        if: needs.deploy-environment.result == 'success'
        run: |
          echo "✅ Deployment to ${{ inputs.environment }} completed successfully!"
          echo "🔗 Check the Azure portal for deployed resources"

      - name: Deployment Failure
        if: needs.deploy-environment.result == 'failure'
        run: |
          echo "❌ Deployment to ${{ inputs.environment }} failed!"
          echo "Please check the workflow logs for more details"
          exit 1
