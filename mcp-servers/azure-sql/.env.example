# Azure SQL Server Example Configuration
# Copy this file to .env and fill in your values

# Azure SQL Database Configuration
AZURE_SQL_SERVER=your-server-name
AZURE_SQL_DATABASE=your-database-name

# Authentication Type: 'azure-ad' (recommended) or 'sql'
AZURE_SQL_AUTH_TYPE=azure-ad

# Only required if using SQL authentication (AZURE_SQL_AUTH_TYPE=sql)
# AZURE_SQL_USERNAME=sqladmin
# AZURE_SQL_PASSWORD=YourPassword123!

# Azure AD Configuration (for azure-ad auth type)
# Ensure you're logged in with: az login
# The MCP server will use DefaultAzureCredential which supports:
# - Azure CLI credentials
# - Managed Identity
# - Environment variables (AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID)
